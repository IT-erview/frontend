const OtherAnswer = (props: { id: number; likeCount: number; like: boolean; content: string; user: string }) => {
  return (
    <button>
      <div></div>
    </button>
  )
}

export default OtherAnswer
